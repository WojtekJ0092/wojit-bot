# Environment variables — copy to .env and fill in real values
# See docs/backend architicture.md §12

VITE_API_BASE=https://your-api
VITE_PUBLIC_TOKEN=your-public-token-here

# Set to "true" to use local JSON fixtures instead of the real API
VITE_USE_FIXTURES=true
