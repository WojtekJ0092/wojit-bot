// ---------------------------------------------------------------------------
// App — ski map only mode (chatbot temporarily removed)
// ---------------------------------------------------------------------------

import { CursorTrail, GateGame, NukeEgg } from "@/components";

export default function App() {
  return (
    <>
      <CursorTrail />
      <NukeEgg />
      <GateGame />
    </>
  );
}
